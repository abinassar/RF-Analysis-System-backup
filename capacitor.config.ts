import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RF-Analysis-System',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
